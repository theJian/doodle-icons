import 'package:flutter/widgets.dart' as widgets;
import 'package:flutter_svg/flutter_svg.dart';

class ${componentName} extends widgets.StatelessWidget {
  final widgets.Color? color;
  final double size;
  final double? width;
  final double? height;

  const ${componentName}({
    super.key,
    this.color,
    this.size = 24,
    this.width,
    this.height,
  });

  @override
  widgets.Widget build(widgets.BuildContext context) => widgets.SizedBox(
    width: width ?? size,
    height: height ?? size,
    child: SvgPicture.string(
      r'''
${svg}''',
      colorFilter: color != null
          ? widgets.ColorFilter.mode(color!, widgets.BlendMode.srcIn)
          : null,
    ),
  );
}
